__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/
venv/
.venv/
.env
*.log
*.jsonl
# SQLite analysis index (tools/ingest.py) — derived, rebuildable, never committed.
*.db
*.sqlite
*.sqlite3
.DS_Store

# acme.sh TSIG key for DNS-01 challenge — secret, do not commit.
acme/tsig.key

# Operator-specific "our infra" IP list for tools/morning_report.py.
# Varies per operator (VPS, laptops, VPN hosts) — keep gitignored.
tools/our-ips.txt

# Claude Code: ignore everything in .claude/ by default; opt in per-file.
.claude/*
!.claude/settings.json

# Analysis-host secret for tools/annotate.py. NOT the honeypot .env — that one
# has a sibling on the public VPS; an API key does not belong there.
.env.analysis
